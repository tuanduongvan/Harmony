/*
    const successNotification = document.querySelector('.notification-bgr.successfull');
    const errorNotification = document.querySelector('.notification-bgr.error');

    if (type === 'success') {
        successNotification.style.display = 'flex';
    } else if (type === 'error') {
        errorNotification.style.display = 'flex';
    }

    // Adding event listeners to buttons to hide notifications
    document.querySelectorAll('.notification button').forEach(button => {
        button.addEventListener('click', () => {
            button.parentElement.parentElement.style.display = 'none';
        });
    });
*/